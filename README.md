# Mom Health

Static GitHub Pages app for a small family health status page.

The app stores the GitHub fine-grained token only in the local browser and reads
the sanitized JSON file from the private `yihuhou/mom-health-data` repository.
